#include<bits/stdc++.h>
#define INI cin.tie(0)->sync_with_stdio(0);
#define fore(i,a,b) for(int i=a;i<=b;i++)
typedef unsigned long long ull;
typedef long long ll;
using namespace std;
void solve(){
    ll n;cin>>n;int c=0,c1=0;
    vector<ll>vec(n);
    fore(i,0,n-1)
        cin>>vec[i];
    sort(vec.begin(),vec.end());
    fore(i,0,n-1){
        if(vec[i]<vec[i+1])
            c=+
    }
}
signed main(){
    INI solve();
}
